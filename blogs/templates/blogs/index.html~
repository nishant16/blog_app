{% extends 'blogs/base.html' %}
{% block title %}Welcome to my blog{% endblock %}

{% block content %}
    <h2>Categories</h2>
    {% if categories %}
        {% for each in categories %}
            <li><a href="{% url 'n1' each.id %}">{{each.title}}</a> </li>
        {% endfor %}

    {% else %}
        <p>there is no posts</p>
    {% endif %}


    <h3>Posts</h3>
    {% if posts %}
        {% for each in posts %}
            <li> {{each.title}}</a> </li>
        {% endfor %}
    {% else %}
        <p>there are no posts</p>
    {% endif %}
{% endblock %}
